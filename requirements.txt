# ComfyUI-YuE2 Dependencies
# Automatically installed by ComfyUI-Manager or via: pip install -r requirements.txt

# Audio I/O & High-Fidelity Encoding (Lossless WAV, FLAC 24-bit, MP3 320k)
soundfile>=0.12.1

# Safetensors & Numerical Operations
safetensors>=0.4.0
numpy>=1.24.0

# LLM Producer & Local API integrations (LM Studio, Ollama, OpenAI)
requests>=2.31.0
tiktoken>=0.7.0

# NOTE: PyTorch, TorchAudio, and core YuE2 inference models are handled
# directly by ComfyUI core in-process for maximum speed and memory efficiency.
